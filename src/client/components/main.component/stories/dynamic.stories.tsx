import { Main } from "@/client/components";
import { FC } from "react";

export default {
	title: "components/main/dynamic",
	component: Main
};

export const Standard: FC = () => {
	return <Main />;
};
