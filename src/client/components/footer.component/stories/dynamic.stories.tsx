import { Footer } from "@/client/components";
import { FC } from "react";

export default {
	title: "components/footer/dynamic",
	component: Footer
};

export const Standard: FC = () => {
	return <Footer />;
};
