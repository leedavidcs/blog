import { SpecializeTechnologies } from "@/client/components";
import React, { FC } from "react";

export default {
	title: "components/specialize-technologies/dynamic",
	component: SpecializeTechnologies
};

export const Standard: FC = () => {
	return <SpecializeTechnologies />;
};
